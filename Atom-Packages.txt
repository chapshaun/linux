Install at /home/shaunyboi14/.atom/packages/

Install atom:
sudo add-apt-repository ppa:webupd8team/atom

(Alternative) https://launchpad.net/~webupd8team/+archive/ubuntu/atom/+packages

sudo apt-get update
sudo apt-get install atom

apm install in /packages dir:

	apm install emmet atom-jshint terminal-status minimap

	git clone https://github.com/DanBrooker/file-icons.git
	git clone https://github.com/thomaslindstrom/color-picker

themes:
	git clone https://github.com/zenorocha/dracula-theme.git
	git clone https://github.com/kevinsawicki/monokai

emmet cmds:
  # = id;
  . = class;
  > = child selector;
  + = sibling selector;
  ^ = grouping; () = grouping
  [] = attributes;
  {} = inner content;
  * = mulitiplier;
  $ = padding;
  @ = start at; - = reverse;


p.red.bold#theId$$$@5[title=thetitle]{inner content}*5
